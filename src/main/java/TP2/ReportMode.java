package TP2;

/** ReportMode
 * Enumerado con los distintos tipos de reportes que hay:
 * por consola, en archivo de texto, o todos los disponibles.
 **/

public enum ReportMode {
	Console,
	TextFile,
	AllAvailable
}
